.pull-right
  = link_to 'Edit', edit_course_path(course), class: 'button button-green button-small'
  = link_to 'Delete', course_path(course), method: :delete, data: { confirm: 'Are you sure you want to delete this course?' }, class: 'button button-red button-small'
%h1
  = course.name
%p
  = course.description
