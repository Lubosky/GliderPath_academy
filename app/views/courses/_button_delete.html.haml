- if current_user.is_admin?
  = link_to t('button.delete'),
            course_path(resource),
            method: :delete,
            data: { confirm: 'Are you sure you want to delete this course?' },
            class: 'button button-red'
