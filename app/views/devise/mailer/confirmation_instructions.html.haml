%p
  %b Hi #{@resource.first_name}.

%p Thanks for joining #{Settings.application.name}! It's a real honor to have you as a new student.

%p We have activated your account temporarily, but we still need you to confirm your email address (#{@resource.email}). Please click the following button within 24 hours of receiving this email to activate your #{Settings.application.name} account:

%table{ border: "0", cellpadding: "0", cellspacing: "0", width: "100%" }
  %tr
    %td{ align: "center" }
      = link_to 'Confirm account', confirmation_url(@resource, confirmation_token: @token), class: 'button'

%p Alternatively, you may also copy/paste the following link into your browser:
%b
  %em= confirmation_url(@resource, confirmation_token: @token)

%p And do not forget to share your feedback with us – good and bad, we want it all :smile:. Since we are young and fresh, your feedback is invaluable for us. Get in touch with us via email at #{mail_to Settings.company.email, Settings.company.email}.

%p Happy Learning,

%p
  %b= Settings.application.name
