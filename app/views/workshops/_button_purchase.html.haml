- if resource.is_free?
  = link_to t('button.account.sign_up'), new_user_registration_path, class: 'button button-green' unless user_signed_in?
- else
  = link_to t('button.purchase.with_price', price: resource.price), workshop_purchase_path(workshop_id: resource), class: 'button button-green'
